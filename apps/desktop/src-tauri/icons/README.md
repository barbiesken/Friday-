# Icons

Generated, not committed. Run from `apps/desktop`:

```bash
npx tauri icon path/to/friday-logo.png
```

This writes `32x32.png`, `128x128.png`, `128x128@2x.png`, `icon.icns`,
`icon.ico` and platform variants referenced by `tauri.conf.json`.
